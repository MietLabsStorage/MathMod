clear
syms u
syms v
syms t
syms m_d_t
syms m_p
syms m_s
syms m_t
syms F
syms g
F = (m_s + m_p + m_t - m_d_t* t)*g+(101325-1.0
dv = ((-(v+u) * m_d_t + F) / (m_s + m_p + m_t - m_d_t* t))
v_formul = int(dv, t);
v_formul = abs(solve(v_formul - v, v));
disp('v(t):')
disp(v_formul)